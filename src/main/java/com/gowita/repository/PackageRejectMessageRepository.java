package com.gowita.repository;

import com.gowita.entity.PackageRejectMessageEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PackageRejectMessageRepository extends JpaRepository<PackageRejectMessageEntity, Long> {
}
