package com.gowita.repository;

import com.gowita.entity.TripRejectMessageEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TripRejectMessageRepository extends JpaRepository<TripRejectMessageEntity, Long> {
}
